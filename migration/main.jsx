const root = ReactDOM.createRoot(document.getElementById('root'));

root.render(
  <React.StrictMode>
    <ReactRouterDOM.HashRouter>
      <window.App />
    </ReactRouterDOM.HashRouter>
  </React.StrictMode>
);
